<interfaces>
<% @interface_name.each do |ifd| %>
  <interface>
    <name><%= ifd %></name> 
    <description>"Automation tool configured interface"</description>
  </interface>  
<% end %>
</interfaces>
